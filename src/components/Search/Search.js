import { Input } from "components";
import * as Styled from "./Search.styles";

export const Search = () => {
  return (
    <Styled.SearchContainer>
      <Input />
    </Styled.SearchContainer>
  );
};
